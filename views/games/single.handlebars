<link rel="stylesheet" href="/public/css/game.css">
<div class="center">
    {{#if game}}
    <div class="game">
        <h1>{{game.title}}</h1>
        <img src="{{game.image}}" alt="{{gametitle}}">
    </div>
    {{/if}}
</div>