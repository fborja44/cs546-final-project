const usersData = require('./users');
const gamesData = require('./games');
const reviewsData = require('./reviews');

module.exports = {
  users: usersData,
  games: gamesData,
  reviews: reviewsData
};