const usersData = require('./users');
const gamesData = require('./games');
const reviewsData = require('./reviews');
const replyData = require('./replies');

module.exports = {
  users: usersData,
  games: gamesData,
  reviews: reviewsData,
  replies: replyData
};